# Hints

## Hint 1: Install Dependencies
Run package installation in both `client` and `server` folders.

## Hint 2: Environment Variables
Use the server README as reference for required `.env` values.

## Hint 3: Prisma Migration
Run migration commands from the `server` directory after environment setup.

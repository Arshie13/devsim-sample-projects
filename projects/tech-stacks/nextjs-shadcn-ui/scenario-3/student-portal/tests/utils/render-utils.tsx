/**
 * Shared test utilities for the Student Portal tests.
 *
 * Kept intentionally small — individual level/task test files import what they
 * need directly from the production source under `@/...`. Add helpers here only
 * when at least two test files would otherwise duplicate them.
 */

export const validStudentId = '12-346-78'
export const validPassword = 'sample'

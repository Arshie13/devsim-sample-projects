# Copy this file to ".env" and set your own PostgreSQL connection string.
#   cp .env.example .env
# Then edit USER / PASSWORD / host / database to match your local Postgres.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/gym_member_portal?schema=public"

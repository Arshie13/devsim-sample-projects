// L4-T2 BUG (intentional): this page is intentionally empty.
// Students must:
//   1. Call getMyStreak() from workout.service.ts on mount
//   2. Display currentStreak and longestStreak as numbers
//      (data-testid="current-streak" and data-testid="longest-streak")
//   3. Show lastWorkoutDate in a human-readable format
//   4. Handle the zero/null state gracefully (data-testid="empty-streak")
//
// TODO: implement MyStreak page
export function MyStreak() {
  return (
    <div className="max-w-md mx-auto py-16 text-center text-gray-400">
      My Streak — coming soon!
    </div>
  );
}
